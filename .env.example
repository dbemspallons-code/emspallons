# Configuration locale (ne pas committer les vraies cles)
# Copier ce fichier en .env.local et completer les valeurs

# === SUPABASE (client) ===
VITE_SUPABASE_URL=https://votre-projet.supabase.co
VITE_SUPABASE_ANON_KEY=votre-cle-anonyme-supabase

# === SUPABASE (serveur / Netlify Functions) ===
SUPABASE_SERVICE_KEY=votre-cle-service-role-supabase

# === NETLIFY (CLI / CI) ===
NETLIFY_AUTH_TOKEN=votre-token-netlify
NETLIFY_SITE_ID=votre-site-id-netlify

# === QR / SECURITE ===
QR_TOKEN_SECRET=generer-avec-openssl-rand-hex-32

# === JOBS PLANIFIES ===
SCHEDULED_JOB_SECRET=secret-partage-pour-cron

# === TWILIO (optionnel) ===
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_FROM=whatsapp:+14155238886

# === SMTP (optionnel) ===
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
